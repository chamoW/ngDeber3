import { ShowEraseDirective } from './show-erase.directive';

describe('ShowEraseDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowEraseDirective();
    expect(directive).toBeTruthy();
  });
});
